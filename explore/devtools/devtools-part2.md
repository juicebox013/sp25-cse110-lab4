# Question 1
The bug was that the code was incorrectly converting the values of num1 and num2. Instead of adding both together as integers, the function was concatenating both variables as strings.

# Question 2
I would fix this error by hardcoding a conversion of num1 and num2 to numbers in calculateSum. 